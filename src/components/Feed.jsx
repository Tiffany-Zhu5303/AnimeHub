import React, {useState} from "react";
import { supabase } from "../client";
import "./Feed.css";

const Feed = () => {
    return(
        <div className="feed">

        </div>
    );
};

export default Feed;